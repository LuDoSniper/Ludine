{% extends 'Layout/base.html.twig' %}

{% block title %}
    Login
{% endblock %}

{% block body %}
    {{ form_start(form) }}
    {{ form_rest(form) }}
    <button>Connexion</button>
    <a href="{{ path('app_login_google') }}">Se connecter avec Google</a>
    {{ form_end(form) }}
    <a href="{{ path('app_register') }}">S'inscrire</a>
{% endblock %}